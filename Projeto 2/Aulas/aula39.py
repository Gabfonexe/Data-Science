# Criar uma lista de frutas a partir do input do usuario

frutas_usuario = input('Digite o nome das frutas separadas por virgula: ')
frutas_lista = frutas_usuario.split(', ') # corta as strings quando verifica se ha virgula e espaço

print(frutas_lista)
